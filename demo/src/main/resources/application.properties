# Supabase Session Pooler (5432 ??, IPv4 ??)
spring.datasource.url=${DB_URL}&currentSchema=smart-star

# username: ?????? ??? ??? (postgres. + project-ref)
spring.datasource.username=${DB_USERNAME}

# ??? password: Supabase Database Settings ? Reset password ?? ?? ??? ? ???
# Reset ? ?? ???? (??/??? ??!)
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Dialect ?? ?? + ?? ?? ?? (startup ?? ?? ??)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ????? ??? ?? ???? (Supabase Pooler + Hibernate 6+ ??)
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ?? ??? & ddl-auto
# 1. ???? ????? ?? ??? ?? (Supabase ?? ??)


# 2. Hibernate? ???? ? ?? ??? ??? ??? ???? ?? ??
spring.jpa.properties.hibernate.default_schema=smart-star
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# HikariCP ? ??? (startup ? ?? ?? ??)
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.leak-detection-threshold=2000
spring.datasource.hikari.connection-test-query=SELECT 1